/// <reference types="vite/client" />

// Injected by vite `define` from the repo-root VERSION file (see vite.config.ts).
// Declared here so tsc knows the global; the value is a plain string at build time.
declare const __APP_VERSION__: string;
